{
  "name": "contextos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first memory operating system for AI agents.",
  "scripts": {
    "start": "node src/app.js",
    "modeld": "node src/modeld/server.js",
    "demo": "echo '⚠️  Use: npm run demo:safe' && exit 1",
    "demo:safe": "node scripts/demo.js",
    "test": "node --test",
    "lint": "npx eslint src/ test/"
  },
  "engines": {
    "node": ">=25.0.0"
  },
  "dependencies": {
    "node-llama-cpp": "^3.17.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.24.0",
    "eslint": "^9.24.0"
  }
}
